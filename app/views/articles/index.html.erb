<h1>Wikis</h1>

<% @articles.each do |article| %>
<h2><%= link_to article.title, article_path(article) %></h2>
<p><%= article.content %></p>
<%= link_to "Un job de plus en moins!", article_path(article), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %>
<% end %>

<p><%= link_to "Encore plus de connaissances ?", new_article_path %></p>
